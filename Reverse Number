#include<stdio.h>
int main(){
    int n,rem;
    printf("Enter a number :");
    scanf("%d",&n);
    int r = 0;
    while(n!=0){
        rem = n%10;
        r = r*10+rem;
        n = n/10;
    }
    printf("The reversed number is %d" ,r);
return 0;
}

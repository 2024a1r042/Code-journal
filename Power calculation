#include<stdio.h>
int main(){
    int base,power,value=1;
    printf("Enter number and its power:");
    scanf("%d%d",&base,&power);
for(int i=1;i<=power;i++)

{
    value*=base;
}
printf("\n%d^%d=%d",base,power,value);
return 0;
}
